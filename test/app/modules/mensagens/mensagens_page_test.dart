import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:whatsflutter/app/modules/mensagens/mensagens_page.dart';

main() {
  testWidgets('MensagensPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MensagensPage(title: 'Mensagens')));
    //  final titleFinder = find.text('Mensagens');
    //  expect(titleFinder, findsOneWidget);
  });
}
