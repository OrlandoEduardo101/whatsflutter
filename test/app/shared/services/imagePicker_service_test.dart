import 'package:flutter_test/flutter_test.dart';

import 'package:whatsflutter/app/shared/services/imagePicker_service.dart';

void main() {
  ImagePickerService service;

  setUp(() {
//    service = ImagePickerService();
  });
//
  group('ImagePickerService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<ImagePickerService>());
//    });
//
  });
}
